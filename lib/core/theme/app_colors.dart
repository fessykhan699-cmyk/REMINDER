import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Backgrounds ──
  static const Color backgroundPrimary = Color(0xFF0F1115);
  static const Color backgroundSecondary = Color(0xFF151821);

  // ── Cards / Glass ──
  static const Color cardBackground = Color(0x0AFFFFFF);
  static const Color cardBorder = Color(0x14FFFFFF);
  static const Color glassFill = Color(0x0FFFFFFF);
  static const Color glassBorder = Color(0x1AFFFFFF);

  // ── Text — warm cream palette ──
  static const Color textPrimary = Color(0xFFE8E2D6);
  static const Color textSecondary = Color(0xFFA1A5AB);
  static const Color textMuted = Color(0xFF7A8088);

  // ── Accent — gold ──
  static const Color accent = Color(0xFFC9A86A);
  static const Color accentPrimary = accent;
  static const Color accentSecondary = accent;

  // ── Semantic ──
  static const Color success = Color(0xFF5FA777);
  static const Color warning = Color(0xFFD9A441);
  static const Color danger = Color(0xFFC45C5C);
}
